<Window x:Class="MercadoPagoScraper.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="MercadoPago Scraper" Height="400" Width="600">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Selección de archivo -->
        <StackPanel Orientation="Horizontal" Grid.Row="0" Margin="0,0,0,10">
            <Button Name="btnSelectFile" Content="Seleccionar Excel" Width="150" Click="BtnSelectFile_Click"/>
            <TextBox Name="txtFilePath" Width="400" Margin="10,0,0,0" IsReadOnly="True"/>
        </StackPanel>

        <!-- Fila inicial -->
        <StackPanel Orientation="Horizontal" Grid.Row="1" Margin="0,0,0,10">
            <Label Content="Fila inicial:" VerticalAlignment="Center"/>
            <TextBox Name="txtStartRow" Width="100" Margin="10,0,0,0" Text="2"/>
        </StackPanel>

        <!-- Botón procesar -->
        <Button Name="btnProcess" Grid.Row="2" Content="Procesar" Width="100" Click="BtnProcess_Click"/>

        <!-- Logs -->
        <TextBox Name="txtLogs" Grid.Row="3" Margin="0,10,0,0" AcceptsReturn="True" VerticalScrollBarVisibility="Auto"/>
    </Grid>
</Window>